# STATE — сайт ConvoyMind

Формат задан `CLAUDE.md` §7. Обновляется после каждого завершённого шага.
После `/clear` читать по порядку: `CLAUDE.md`, `docs/LANDING_PLAN.md`, `STATE.md`, `docs/DECISIONS.md`, `NOT_NOW.md`, `docs/COPY.md` — и пересказать состояние в 5 строках до начала работы. Пути важны: план, решения и текст лежат в `docs/`, а `STATE.md` и `NOT_NOW.md` в корне.

## Цель сайта

Одностраничный польский лендинг, единственный сценарий: диспетчер зашёл → узнал свой день → оставил заявку на пилот. Результат = письмо на почту, а не «красивый сайт».

## Обещание страницы

Вы видите, где график ломается — до выезда, а не на 400-м километре.

## Сделано

- [2026-09-08] Прочитаны `CLAUDE.md`, `docs/LANDING_PLAN.md`, `docs/DECISIONS.md` и весь код. В репозитории голый скелет `create-next-app`: Next 16.3.4, React 19.2.8, Tailwind v4, pnpm.
- [2026-09-08] Владелец закрыл три вопроса: почта — Resend; юридические данные есть и будут переданы; `dynamic` меняем с `'force-static'` на `'error'`.
- [2026-09-08] Владелец создал `.mcp.json` с `next-devtools-mcp`. Файл валиден, совпадает с образцом документации, пакет на npm есть (`0.4.0`) `[проверено]`. Подключится после перезапуска Claude Code при запущенном `pnpm dev`.
- [2026-09-08] Шаг 0: созданы `STATE.md` и `NOT_NOW.md` — без них правило перезапуска из `CLAUDE.md` §7 не работало.
- [2026-09-08] Шаг 1 закрыт. `app/globals.css` — токены §3 дословно (`@theme` для шрифтов, `:root` + `@theme inline` для цвета); `app/layout.tsx` — Montserrat 600/700 с `subsets: ['latin','latin-ext']`, `<html lang="pl">`, Geist убран; `app/page.tsx` — заглушка с `export const dynamic = 'error'`; заглушки create-next-app и `public/*.svg` удалены; в `.gitignore` добавлен `!.env.example`.
- [2026-09-08] Правки в `LANDING_PLAN` §2 и `DECISIONS.md`: `'force-static'` заменён на `'error'`, старая строка помечена ОТМЕНЕНО.
- [2026-09-08] Шаг 2: написан `docs/COPY.md` — весь польский текст S0–S7 плюс тексты ошибок формы, каждый блок с русской подстрочной глоссой. Обращение `Ty` (решение владельца). Проверки прогнаны скриптом: «он» 37 против «мы» 13, причастных оборотов 0, запрещённых слов 0, число на странице одно и стоит рядом со словом `cel`.
- [2026-09-08] Правки владельца по `COPY.md`: открытая формула в S5 убрана (масштаб теперь словами), `kurochka265@gmail.com` вписан в текст ошибки и в футер, добавлен раздел S7 «Kto za tym stoi» с тремя реальными именами — форма стала S8. `DECISIONS.md` и `NOT_NOW.md` обновлены.
- [2026-09-08] Второй круг правок по команде: `EcoFlow` (старое название продукта) на сайт не выносим; биография Микиты дописана владельцем; PPC и продажи в биографию Владислава не берём; фото не ставим.
- [2026-09-08] Шаг 3: свёрстаны S0–S3. `app/_sections/Header.tsx`, `Hero.tsx`, `Problem.tsx`, `HowItWorks.tsx`, собраны в `app/page.tsx`. Приватная папка `_sections` — соглашение Next 16 `[проверено]`, `01-getting-started/02-project-structure.md:259`. Все компоненты серверные, `'use client'` нет ни одного.
- [2026-09-13] Шаг B/C закрыт по коду и по проверкам. `Contact` и `Footer` смонтированы в `app/page.tsx` — до этого обе секции лежали в репозитории, но на странице не рендерились, и кнопка `Umów rozmowę` в шапке вела в никуда. Создан `.env.example` (`RESEND_API_KEY`, `RESEND_FROM`, `CONTACT_INBOX`, `NEXT_PUBLIC_UMAMI_WEBSITE_ID`); `git status` его видит, исключение `!.env.example` работает `[проверено]`.
- [2026-09-13] Аналитика снята с повестки (решение владельца): удалены `app/_lib/track.ts`, вызовы в форме, атрибут `data-umami-event`, переменная из `.env.example`. Мёртвой обёртки не осталось.
- [2026-09-13] Приём заявки переведён на письмо. `app/_sections/Contact.tsx` — секция с кнопкой `mailto:` (тема `Pilotaż ConvoyMind`) и видимым адресом рядом; форма переехала в `ContactForm.tsx` с комментарием, что она припаркована и чем расконсервируется. Футер переписан: почта плюс тихая строка про этап пилота и инкубатор Mobiscale. Тексты обновлены в `docs/COPY.md` (S8 и «Футер»).

- [2026-09-13, вечер] Второй круг: текст и оживление. Владелец: «текст слипся», «сайт должен ожить, интерфейс видит пользователя», разрешил анимационную библиотеку. Сделано:
  - Текст (`docs/COPY.md`): S2 карточка 3 начинается с `Tachograf nie negocjuje.`; S4 вывод разбит на два предложения; S5 сокращён с 12 строк до 6 (ушли повторы S2, `Nie mamy jeszcze…`, `Po fakcie zostaje liczenie strat`); из футера убран адрес — он один, в S8. Все спорные вырезы — решения владельца.
  - Типографика: в каждом блоке S2/S4/S5 одна поднятая строка (Montserrat) и тихая (`--text-muted`); в S4 акцентные слова `--primary`, «кто умеет» — плашки.
  - `motion` 13.2.0 (`app/_motion/`): `MotionProvider` (LazyMotion + domAnimation + reducedMotion), `Reveal`/`RevealItem` (лесенка при появлении), `CtaLink` (scale 1.02/0.98), `NavLink` (подчёркивание слева направо), `useReveal` (прячем только то, что ниже сгиба, только на клиенте, не при reduced-motion). Вход hero — CSS `.hero-word`/`.hero-in` с пружиной `linear()` (h1 = LCP).
  - Схема S3 (`HowItWorksLive.tsx`, клиентский): четыре такта, номера шагов загораются в такт, мобильный список играет ту же последовательность. Времён суток нет.
  - Токены: `--primary-soft`, `--primary-line` (прозрачный акцент), `--ease-spring`.
  - Решения записаны: `DECISIONS.md` (7 строк + 2 ОТМЕНЕНО), `NOT_NOW.md`, `LANDING_PLAN` §2 и §5 S3, `CLAUDE.md` §2 (оговорка про `motion`).

## Сейчас в работе

Страница целиком собрана, ожила и проверена: S0–S8 плюс футер. Заявка принимается письмом.

Проверено 2026-09-13 вечером `[проверено]`:
- `pnpm build` зелёный, `/` = `○ (Static)`; `pnpm lint` чист.
- JS страницы: **173 → 217 КБ gz** (+44 КБ — `motion`), метод: сумма gzip чанков из `<script src>` в HTML. CSS 5,5 КБ gz.
- Lighthouse mobile (прод-сборка на localhost, Brave headless, симуляция 4G/CPU×4): **Performance 98, Accessibility 100**, FCP 0,8 с, LCP 2,4 с, TBT 0, CLS 0 — два прогона. До изменений на том же стенде: 99 / 100, LCP 2,0 с. Разница LCP — цена появления слов hero; шаг ужат с 45 до 30 мс, на 45 мс было 2,8 с.
- В SSR-разметке ничего не спрятано (все inline-стили `opacity:1`), без JS страница полная; ниже сгиба прячется только после гидрации — проверено в браузере. Ошибок гидрации в консоли нет.
- Глазами в браузере (1430 px и iframe 360 px): слова hero, лесенка карточек, схема все четыре такта с загорающимися номерами, мобильная вертикальная схема, подчёркивание nav, подъём карточек, S4–S8, футер без адреса. 360 px: `scrollWidth - clientWidth = 0`.
- `prefers-reduced-motion`: правило `animation:none` для `.hero-word,.hero-in` есть в собранном CSS; `useReveal` при reduced-motion не прячет ничего (по коду), `MotionConfig reducedMotion="user"`. Эмуляцию reduced-motion в браузере инструментом включить не смог — проверено по CSS и коду, не по рендеру.
- Пересчёт §5.5 по отрендеренной странице: **он 30, мы 10** (было 35/12 — текст стал короче, перевес 3:1 сохранился). Числа те же (`1–5`, `22:40`, `45`, `30`, `265`), `±30 minut` один раз, `cel` два раза рядом. Деепричастий ноль, запрещённых слов ноль, `mailto:` два (кнопка и адрес в S8), адрес на странице один.

## Следующий шаг

Показать владельцу вживую (`pnpm dev` у него запущен на 3000) и собрать правки по ощущению движения: скорость лесенки, сила подъёма карточек, темп схемы. Потом — как раньше: Vercel и прод, затем `metadata`/OG/`robots`/`sitemap` (упирается в домен).

## Открытые вопросы ко мне

- **Домен.** Самое узкое место: без него нет ни `metadata.canonical`, ни `robots.ts`/`sitemap.ts`, ни почты на своём домене. `kurochka265@gmail.com` в польском B2B читается слабее, чем `kontakt@<домен>`.
- **Аккаунт Resend.** Регистрация на `kurochka265@gmail.com`, ключ и три переменные в Vercel — и форма возвращается на страницу одной строкой. Бесплатный тариф `[проверено]`, `resend.com/pricing`: 3000 писем в месяц, 100 в день.
- **Юрлицо.** Как появится — реквизиты встают в футер, а вместе с ними возвращается вопрос про политику приватности и администратора данных.
- **Вычитка носителем.** Весь польский текст её ещё не проходил — требование `CLAUDE.md` §2. Это последний шаг перед тем, как показывать сайт перевозчикам. 2026-09-13 добавились 5 новых строк и две подписи схемы — список в `COPY.md`, открытый вопрос 5.
- **`react-spring` vs `motion`.** Ты сказал «библиотека spring»; я взял `motion` (причины в `DECISIONS.md`). Если имелся в виду именно `@react-spring/web` — скажи, обёртки в `app/_motion/` переписываются, секции не трогаются.
