# STATE — сайт ConvoyMind

Формат задан `CLAUDE.md` §7. Обновляется после каждого завершённого шага.
После `/clear` читать по порядку: `CLAUDE.md`, `LANDING_PLAN.md`, `STATE.md`, `DECISIONS.md`, `NOT_NOW.md` — и пересказать состояние в 5 строках до начала работы.

## Цель сайта

Одностраничный польский лендинг, единственный сценарий: диспетчер зашёл → узнал свой день → оставил заявку на пилот. Результат = письмо на почту, а не «красивый сайт».

## Обещание страницы

Вы видите, где график ломается — до выезда, а не на 400-м километре.

## Сделано

- [2026-09-08] Прочитаны `CLAUDE.md`, `docs/LANDING_PLAN.md`, `docs/DECISIONS.md` и весь код. В репозитории голый скелет `create-next-app`: Next 16.3.4, React 19.2.8, Tailwind v4, pnpm.
- [2026-09-08] Владелец закрыл три вопроса: почта — Resend; юридические данные есть и будут переданы; `dynamic` меняем с `'force-static'` на `'error'`.
- [2026-09-08] Владелец создал `.mcp.json` с `next-devtools-mcp`. Файл валиден, совпадает с образцом документации, пакет на npm есть (`0.4.0`) `[проверено]`. Подключится после перезапуска Claude Code при запущенном `pnpm dev`.
- [2026-09-08] Шаг 0: созданы `STATE.md` и `NOT_NOW.md` — без них правило перезапуска из `CLAUDE.md` §7 не работало.
- [2026-09-08] Шаг 1 закрыт. `app/globals.css` — токены §3 дословно (`@theme` для шрифтов, `:root` + `@theme inline` для цвета); `app/layout.tsx` — Montserrat 600/700 с `subsets: ['latin','latin-ext']`, `<html lang="pl">`, Geist убран; `app/page.tsx` — заглушка с `export const dynamic = 'error'`; заглушки create-next-app и `public/*.svg` удалены; в `.gitignore` добавлен `!.env.example`.
- [2026-09-08] Правки в `LANDING_PLAN` §2 и `DECISIONS.md`: `'force-static'` заменён на `'error'`, старая строка помечена ОТМЕНЕНО.
- [2026-09-08] Шаг 2: написан `docs/COPY.md` — весь польский текст S0–S7 плюс тексты ошибок формы, каждый блок с русской подстрочной глоссой. Обращение `Ty` (решение владельца). Проверки прогнаны скриптом: «он» 37 против «мы» 13, причастных оборотов 0, запрещённых слов 0, число на странице одно и стоит рядом со словом `cel`.
- [2026-09-08] Правки владельца по `COPY.md`: открытая формула в S5 убрана (масштаб теперь словами), `kurochka265@gmail.com` вписан в текст ошибки и в футер, добавлен раздел S7 «Kto za tym stoi» с тремя реальными именами — форма стала S8. `DECISIONS.md` и `NOT_NOW.md` обновлены.
- [2026-09-08] Второй круг правок по команде: `EcoFlow` (старое название продукта) на сайт не выносим; биография Микиты дописана владельцем; PPC и продажи в биографию Владислава не берём; фото не ставим.
- [2026-09-08] Шаг 3: свёрстаны S0–S3. `app/_sections/Header.tsx`, `Hero.tsx`, `Problem.tsx`, `HowItWorks.tsx`, собраны в `app/page.tsx`. Приватная папка `_sections` — соглашение Next 16 `[проверено]`, `01-getting-started/02-project-structure.md:259`. Все компоненты серверные, `'use client'` нет ни одного.

## Сейчас в работе

Шаги 0–3 закрыты и проверены.

Что проверено на шаге 3 `[проверено]`:
- `pnpm build` зелёный, `/` по-прежнему `○ (Static)`; `pnpm lint` без замечаний;
- весь польский текст S0–S3 есть в HTML из `curl` (7 фраз из разных секций, каждая найдена) — пункт 2 критерия;
- на настоящем вьюпорте 360 px горизонтального скролла нет (`scrollWidth - clientWidth = 0`), за пределы экрана не выходит ни один элемент;
- на 360 px в шапке остаются только логотип и кнопка (`nav` → `display: none`), карточки S2 встают в колонку по 320 px;
- схема маршрута на мобильном вертикальная, на десктопе горизонтальная; точки и линия совпадают до пикселя (все четыре точки и центр линии на x = 46.0).

Оговорка по методу: окно Chrome на macOS не сжимается до 360 px, поэтому мобильную ширину мерил в iframe шириной 360 px на том же документе — это настоящий вьюпорт, а не эмуляция.

Что проверено на шаге 1 `[проверено]`:
- `pnpm build` зелёный, `/` помечена `○ (Static)` — пункт 1 критерия;
- `curl -s localhost:3000 | grep -c "ĄĆĘŁŃÓŚŹŻ"` → 1, `<html lang="pl">` — пункт 2 критерия;
- latin-ext подмножество Montserrat реально загружено: face с `unicode-range: U+100-2BA,…` имеет `status: "loaded"`, кириллический и вьетнамский остались `unloaded`; ширина строки `ąćęłńóśźż` 241.08 px против 191.93 px в системном шрифте — подстановки нет. Скриншот подтверждает — пункт 6 критерия;
- `dynamic = 'error'` роняет сборку: временно внесли `await headers()`, получили `Error: Route / with dynamic = "error" couldn't be rendered statically because it used headers()`. Файл возвращён, сборка снова зелёная.

## Следующий шаг

Шаг 4: секции S4 (таблица «чего не хватает»), S5 (что получаете), S6 (пилот), S7 (кто за этим стоит). Текст из `COPY.md` дословно.
Главное на этом шаге — таблица S4 на 360 px обязана превратиться в три карточки, а не скроллиться вбок (`LANDING_PLAN` §7).

Известный временный дефект: якоря `#pilotaz` и `#kontakt` в шапке пока никуда не ведут — целевые секции появятся на шагах 4 и 5.

## Открытые вопросы ко мне

- Адрес ящика, на который падают заявки (`CONTACT_INBOX`). Блокирует доведение шага 5 до реального письма.
- Название юрлица, адрес, контактный email. Блокируют шаг 6 — политику приватности и футер.
- Домен сайта. Нужен для `metadata.canonical`, `robots.ts`, `sitemap.ts` и для DNS-записей Resend.
- Юрлицо и адрес для футера и политики приватности. Email дан: `kurochka265@gmail.com`.
- По разделу команды: что такое `EcoFlow` в биографии Максима; чем заканчивается оборванная фраза в биографии Микиты; ставим ли фото.
- Домен. Нужен и для `metadata`/`robots`/`sitemap`, и для отправки писем: Resend без подтверждённого домена шлёт только с тестового адреса `[предположение]`, проверяется в панели Resend.
